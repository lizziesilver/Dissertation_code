# get most recent ids
last_id_log_file = "log/last_id_log.txt"

# log run
sim_run_log_file = "log/simulation_run_log.txt"

# Progenitor graph config: 
num_nodes = 6
prob_edges = 0.4
graph_gen_log_file = "log/graph_generation_log.txt"

# Evolved graph config:
num_evolved = 3
num_mods = 3

# Data gen config:
parameterization = "linear_gaussian"
sample_size = 100

# Data pooling config:
pooled_sample_sizes = c(80, 80, 80)
